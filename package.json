{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:make": "make build",
    "clean": "make clean",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "release": "make build && changeset publish",
    "version": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "@types/moo": "^0.5.7",
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "tsconfig": "workspace:*",
    "turbo": "latest"
  },
  "dependencies": {
    "moo": "^0.5.2",
    "tsup": "^7.2.0"
  },
  "packageManager": "pnpm@8.6.10",
  "name": "artemis-repo",
  "version": "0.0.13"
}
